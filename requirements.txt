langgraph
langchain
langchain-openai
openai
pillow
python-dotenv
flask
werkzeug
fal-client
requests
httpx
tavily-python
gunicorn==21.2.0

# PDF Conversion & Processing Libraries
pypdf
PyMuPDF
img2pdf
pdf2image
python-docx
openpyxl
python-pptx
# New PDF Features (Oct 2025)
pikepdf          # PDF compression
pdfplumber       # Table extraction, PDF to CSV/HTML
reportlab        # CSV to PDF generation
weasyprint       # HTML to PDF conversion
# Image Format Conversions (Oct 2025)
pillow-heif      # HEIC/HEIF to JPEG conversion (iPhone photos)

# Phase 1 & 2 Tools (Oct 2025 - 1.65M monthly searches!)
# Text Tools - No external dependencies needed (pure Python)
# QR Codes & Barcodes (400K monthly searches)
qrcode[pil]      # QR code generation with PIL image support
python-barcode   # Barcode generation (EAN, UPC, Code128, etc.)

# TEMPORARILY DISABLED - Audio/Video tools causing deployment issues
# # Audio Tools (400K monthly searches)
# pydub            # Audio processing and conversion
# # Note: Requires ffmpeg to be installed on system
#
# # Video Tools (350K monthly searches)
# moviepy          # Video editing and conversion
# imageio          # Image/video I/O (used by moviepy)
# imageio-ffmpeg   # FFmpeg binary for imageio (auto-installs ffmpeg)